# v0.9.9 Scope Plan

> # ⛔ HOLD — awaiting user GO
> No code. No `patches/v0.9.9` execution. This doc + the retrospective + GH issue tagging only.
> Source: v0.9.8 live audit (112 findings, GH issues #185–277, all labeled `v0.9.9`). Companion: `docs/superpowers/audit-v0.9.8.md`.

**Counts:** 112 findings · 93 open issues · 13 tagged `regression`/`PARTIAL` · ~6 P0 · ~22 P1 · rest P2/P3.
**Constraints (locked):** branch `patches/v0.9.9` · single PR · no direct main · **GitHub-hosted runners only** · Biome 0-err · i18n en/es/ar for new strings · zero-deferral · **full `pnpm vitest run` per group gate** · **NEW: e2e UI-acceptance gate** (route-reachability Playwright smoke + per-feature deployed-image checklist — see retrospective).
**Migrations anticipated:** 0062 notification types (#195) · 0063 cover backfill (#214) · 0064 avatar column (#199, if in-scope) · row-body if #241 row-detail.

---

## G0 — Regressions & unfulfilled prior scope (BUILD FIRST)
Features marked "done" in earlier releases that never passed UI acceptance. Lead the release.

| # | Title | P | Regression | Cause hypothesis | Fix sketch |
|---|-------|---|-----------|------------------|-----------|
| #251 | Sign out broken | P0 | v0.1.0 | Sidebar form POSTs CSRF-less to `/api/auth/signout`; Auth.js v5 rejects; exported `signOut()` never invoked; no `/logout` | Wire `signOut()` server action (config.ts:220) or client `signOut()` from next-auth/react; fix duplicate in sessions-card.tsx:121 |
| #252 | Comment renders raw `@[Jon](uuid)` | P0 | v0.3.0 | comment-panel.tsx:189 renders `{comment.body}` raw; mention parser never applied to read path | parse-and-render util on comment bodies → mention pills |
| #254 | Slash parser leak (+#38/76/77/111/112) | P0 | v0.9.6 | slash-extension.ts:786 sync unconditional `deleteRange(range)` + async/early-return `props.command`; range excludes `/` in non-paragraph blocks; corrupts persisted hierarchy (stray char→heading→/toc) | restore-on-cancel; range that bounds the trigger; `preventDefault` on Enter; await dialog/lazy resolution before delete |
| #185 | `/settings/workspace/general` 500 | P0 | pre-0.9 | bare `.select()` includes `workspaces.icon`; live DB missing migration 0054 (42703) | **ops: redeploy+migrate** + code-harden: narrow select, `settings/error.tsx`, entrypoint fail-loud on pending |
| #259 | Per-page permissions UI missing | P1 | v0.7.0 headline | backend shipped (page_acls 0057 + acl.ts + PageAclManager, mounts share-panel.tsx) but never wired to ⋯ menu | surface SharePanel/PageAclManager from page ⋯ menu (drawer/dialog) |
| #188 | Lock hides Suggest-edits + Bibliography | P1 | v0.9.6×v0.9.8 | editor.tsx:122/560 gates both on `effectiveEditable`(=!locked) | keep visible-disabled; gate Bibliography on `editable` alone |
| #214 | Legacy orange covers not backfilled | P1 | PARTIAL v0.9.8 | palette shipped new-pages-only; existing rows keep legacy orange cover descriptor | migration 0063: reassign curated preset to pages whose cover = legacy orange |
| #189 | Toolbar no hover tooltips | P2 | v0.9.7 | aria-labels exist; no `title`/Tooltip (v0.9.7 labeled automation builder, not this bar) | add Tooltip to Comments/History/Lock triggers |
| #190 | Submit-for-review hierarchy | P2 | v0.9.7 | both buttons `variant=outline` already | make submit-for-review `variant=default` |
| #266 | Saved-search sidebar no live-update | P2 | v0.9.7 | no refresh on save | router.refresh()/optimistic insert (refetch-gap, see G11) |
| #264 | DB add-view types grayed unexplained | P2 | v0.9.6 | disabled Calendar/Timeline/Board unannotated | ship them OR remove OR "(coming soon)" |
| #256 | Version-history drawer no refetch | P2 | v0.5.0 | save toasts but drawer stale | refetch/optimistic on save (refetch-gap, see G11) |

---

## G1 — Critical (non-regression P0)
Covered above (#185/#251/#252/#254 lead). Remaining new-P0-adjacent:

| # | Title | P | Cause | Fix |
|---|-------|---|-------|-----|
| #270 | Approval → raw "Decision failed (409)" | P1 | self-approval or stale-state conflict, no friendly copy | map 409 → "you cannot approve your own review" / "already resolved" |
| #272 | Approval 409 never auto-clears | P2 | error persists indefinitely | auto-dismiss 5s or clear on next action |
| #271 | Block right-click context menu missing | P1 | no contextmenu handler; native suppressed | block context menu (reuse ⋮⋮ handle actions: Duplicate/Delete/Comment/Convert/Color/Move/Copy-link) |

---

## G2 — Sidebar shell rebuild (cohesive — `sidebar-content.tsx` + `virtualized-page-tree.tsx` + `(app)` shell)
| # | Title | P | Fix |
|---|-------|---|-----|
| #207 | Sidebar not sticky | P1 | `position:sticky;top:0;height:100vh` or fixed shell, main column sole scroll container |
| #208 | Sidebar too tall/not compact | P2 | 28-32px rows, 14px text, 16px icon, py-1, tighten section gap |
| #209 | PAGES tree fixed-px not flex | P2 | tree container `flex-grow:1` |
| #210 | Default OS scrollbar in tree | P2 | thin themed overlay scrollbar (`scrollbar-width:thin` + webkit, `--border-muted`, `scrollbar-gutter:stable`) |
| #211 | Scroll chaining bubbles to doc | P2 | `overscroll-behavior:contain` on tree |
| #213 | No expand-all/collapse-all | P2 | "collapse all" icon next to + in PAGES header |
| #212 | No scroll-position affordance | P3 | sticky parent header or left-edge progress |
| #238 | Expand-mode hides sidebar, no re-show | P2 | re-show button / hover hot-edge (ties read-mode G4) |

---

## G3 — Nav surfaces & taxonomy
| # | Title | P | Fix |
|---|-------|---|-----|
| #186 | Chat-bridge outside settings hub + dup nav | P1 | relocate `/admin/chat-bridge`→`/settings/admin/chat-bridge` + redirects + dedupe entries |
| #202 | /favorites no sidebar entry | P2 | Star `<Link href=/favorites>` before My tasks in sidebar-footer-nav.tsx:28 |
| #187 | Export: three terms one page | P2 | align nav/breadcrumb/heading |
| #196 | Connectors two "connectors" sections | P2 | rename → "Database sync" + "Chat bridge" |
| #197 | Connectors inconsistent button labels | P2 | unify verb per section |
| #203 | Quotas empty state needs mint-token link | P2 | `<Link>` to /settings/developer/tokens |
| #225 | "Invite member" → page not modal + label mismatch | P2 | modal OR rename "Manage invites" + pending count |

---

## G4 — Editor polish & content correctness
| # | Title | P | Fix |
|---|-------|---|-----|
| #234 | Outline tiny popover, barely usable | P1 | full drawer like Version history — nested H1/H2/H3 + click-to-scroll |
| #246 | /equation no input affordance | P1 | inline LaTeX ghost text or KaTeX modal w/ live preview |
| #274 | Inconsistent slash ergonomics (modal vs inline) | P1 | pick one pattern (ties #246/#254) |
| #232 | Suggestions drawer no diff preview | P1 | inline diff in card (strikethrough→highlight) |
| #260 | `**bold**` markers not stripped | P1 | bold input rule must delete delimiters |
| #262 | CSS quotes bleed onto blockquote+li | P1 | scope `q` content:quotes to inline only |
| #189 #190 | (in G0) | | |
| #205 | Backlinks unlinked-mention snippets inconsistent | P2 | ts_headline over title‖body or fallback |
| #233 | Suggest-edits chip interaction | P2 | whole chip clickable |
| #235 | Export scattered + missing HTML/DOCX | P2 | one Export menu grouped; add HTML+DOCX |
| #240 | cmd+shift+E export shortcut + ⋯ hints | P2 | register shortcut + show hints |
| #261 | Strikethrough shortcut unsupported | P2 | implement `~~double~~` + document |
| #273 | Toggle collapse ambiguous | P2 | collapsed toggle keeps title, hides only children |
| #239 | Cover image not clickable | P2 | click-cover-to-edit opens picker |
| #275 | Selection toolbar minimal | P1 | add color/highlight/turn-into/comment-on-selection ⌘⇧M/align/sub-sup/inline-math |
| #276 | No heading collapse affordance | P2 | ▾ on heading hover + collapse children |
| #236 #237 | Read-mode tooltips + exit affordance | P2 | tooltip w/ shortcut; floating exit button/banner |

---

## G5 — Database depth
| # | Title | P | Fix |
|---|-------|---|-----|
| #241 | Row click only edits cell, no row-detail | P1 | row-detail panel (drawer/page) w/ all props + body editor; likely row-body schema |
| #242 | Property type labels lowercase/underscored | P1 | proper-case readable labels + i18n |
| #243 | Missing common property types | P1 | add Person/File/Email/Phone/Created-time/Last-edited-time/Created-by/Last-edited-by (schema+coercion+filter/sort+UI) |
| #244 | Add-filter first click no-op (race) | P2 | attach handler after popover open |
| #263 | Gallery add-view no-op (refetch, cf #244) | P2 | ensure view-create fires + tab appears (refetch-gap G11) |
| #245 | Row +/drag-handle wrong position | P2 | move to left margin (Notion position) |
| #218 | In-page DB dead space + floating icons | P2 | tighten row container + tooltip icons |
| #264 | (in G0) add-view grayed | | |

---

## G6 — Security & notifications UX
| # | Title | P | Fix |
|---|-------|---|-----|
| #195 | Notif prefs only 2 events; approval/status/lock types absent | P1 | migration 0062: add enum members + emitters + prefs matrix |
| #193 | Encryption self-service misleading copy + no docs | P1 | neutral heading, muted not destructive, reuse e2ee.disabledBody+docsLink (G6 admin pattern) |
| #191 | SSO Add buttons inconsistent | P2 | same variant both |
| #192 | Sessions raw UA + Docker IP | P2 | parse UA; trusted-proxy depth for real IP or hide |
| #194 | SMTP banner no CTA | P2 | admin SMTP link / docs |
| #257 | Webhook events incomplete (CRUD only) | P2 | subscribe to all audited event types |
| #258 | Webhook no select-all/recommended | P2 | bulk-select helpers (depends #257) |
| #265 | Audit-log Actor/Target raw IDs | P2 | resolve user_id→name, entity→title+link |
| #267 | Passkeys leaks env-var names to all | P2 | admin-tier detail only; non-admin generic |
| #277 | Token scope names no tooltip | P2 | per-scope hover tooltip |
| #268 | docs/operations.md plain path | P3 | in-app /docs route or GH link |
| #269 | Audit-log expand on empty metadata | P3 | hide button or "no metadata" |

---

## G7 — Theme correctness
| # | Title | P | Fix |
|---|-------|---|-----|
| #224 | Light-mode regressions | P1 | cover desaturate; approval banner contrast; inline raw text color (ties #38); code/quote blocks theme adoption |
| #223 | Theme toggle dead first click | P2 | simplify dark↔light or explicit 3-state Sun/Auto/Moon |

---

## G8 — Account, theming, cover/icon polish
| # | Title | P | Fix |
|---|-------|---|-----|
| #198 | Profile display name not editable | P2 | client form + PATCH users route |
| #199 | Profile no avatar upload | P2 | avatar upload via /api/files (+ schema 0064?) |
| #200 | Theme tiny swatches + no live preview | P2 | 44px swatches + scoped-CSS-var live preview |
| #201 | Theme hex doesn't show current preset | P2 | seed from ACCENT_PRESETS hex |
| #228 | Cover "Use default" giant primary CTA | P2 | demote to secondary text-link bottom |
| #229 | Cover gradient 6+2 awkward | P2 | 4×2 or 8-wide grid |
| #230 | Cover custom hex no prefill | P2 | prefill current |
| #231 | Icon-picker category icons no tooltips | P2 | accessible category tooltips |

---

## G9 — Onboarding & empty states
| # | Title | P | Fix |
|---|-------|---|-----|
| #206 #215 | New-page literal "Untitled", no autofocus/nudge/naming step | P1 | store empty title; ?new=1→focusTitle+select; optional template prompt |
| #216 | New pages default Published not Draft | P1 | default Draft (check create.ts/0047); security-adjacent |
| #221 | Bell flyout bare empty state | P2 | match /notifications bell-slash empty |
| #222 | Trash/Flashcards/Favorites bare empty states | P2 | icon + one-action CTA each |
| #204 | /favorites empty state no icon | P2 | mirror my-tasks icon+heading |
| #226 | No copy-link after invite creation | P2 | render invite URL + Copy button |
| #227 | Bell flyout close-button low contrast | P3 | fix contrast |

---

## G10 — Templates
| # | Title | P | Fix |
|---|-------|---|-----|
| #248 | Template preview one-line only | P2 | preview drawer w/ rendered blocks or thumbnail |
| #250 | Template card two pills busy | P2 | icon for kind + subtle Built-in |
| #249 | Publish-to-web no URL preview | P2 | show URL before Publish |
| #247 | Expand/focus mode persists across nav | P2 | per-page or reset on new-page |

---

## G11 — Refetch-gap pass (cross-cutting, one standardized fix)
App uses `router.refresh()` (not TanStack), inconsistently applied. WORKS: suggestion-accept, filter 2nd-click. BROKEN: #256 snapshot drawer · #266 saved-search · #263 add-view tabs · cover swatch state. **Audit every mutation for refresh/optimistic-insert; standardize.** Members: #256, #266, #263 (+ v0.8 #178 family).

---

## Build-order recommendation

1. **Pre-build ops (independent of code):** redeploy `ghcr…:v0.9.8` + run entrypoint migrator → clears #185 + the #2/#3/#5 stale-deploy misreports.
2. **G0 regressions first** — lead with the P0 security/correctness: #251 sign-out → #254 slash parser → #252 comment mention → #185 hardening. Then G0 P1 (#259, #188, #214).
   - *Optional:* ship #251 + #254 as standalone hotfixes before the larger PR if security/data-corruption can't wait.
3. **G1 remaining P0/P1** (#270/#271).
4. **P1 by theme:** G4 editor (outline/equation/slash-ergonomics/suggestions-diff/bold/quotes/selection-toolbar) · G5 database depth (row-detail/property-types) · G6 (notif types/encryption copy) · G7 light-mode · G9 onboarding (#206/#216).
5. **G2 sidebar shell rebuild** (cohesive unit).
6. **G11 refetch-gap pass** (one sweep).
7. **P2 by theme:** G3 nav · G8 account/cover/icon · G6 P2s · G10 templates · remaining G4/G5 P2.
8. **P3 last:** #212, #227, #268, #269.
9. **Release:** combined smoke + **e2e UI-acceptance gate (route smoke on deployed image)** + version bump + CHANGELOG + single PR → HOLD for user merge.

Each group gate: lint 0-err · typecheck · i18n none-new · **full vitest** · build · (+ route-reachability smoke for nav/editor groups).
