# deps
node_modules/
.pnpm-store/

# next.js
.next/
out/
next-env.d.ts

# build artifacts
dist/
build/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# tests / coverage
coverage/
.vitest/
playwright-report/
test-results/
# e2e backup-snapshot bundles (item C1; playwright.e2e.config.ts CAIRN_BACKUP_DIR)
.e2e-backups/

# drizzle local
drizzle/.snapshot

# PWA service worker (built by `pnpm build:sw` via @serwist/cli)
public/sw.js
public/sw.js.map

# Emoji dataset (copied from emoji-picker-element-data by `pnpm copy:emoji`)
public/emoji-data.json

# i18n audit per-run report (baseline IS committed; report is regenerated each run)
i18n-audit.report.json

# v0.9.6 G4 (#136): embedding assets staged at build time by scripts/fetch-embedding-assets.mjs
/public/models/
/public/onnx/
