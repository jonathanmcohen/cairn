/**
 * Plan B1 (#138) — task/checkbox list flex layout.
 * Contract stub. Real assertions land with Plan B1.
 * See docs/superpowers/v0.9.14/plan-B-editor-block-fixes.md.
 */
import { describe, it } from 'vitest';

describe('Plan B1 #138 — task-list layout', () => {
  it.todo('blocks.css declares ul[data-type="taskList"] li { display:flex; align-items:baseline }');
  it.todo('task-list label is flex:none (checkbox column does not stretch)');
  it.todo('task-list content div is flex:1 and sits inline with the checkbox');
  it.todo('nested task lists indent without re-stacking the checkbox');
});
