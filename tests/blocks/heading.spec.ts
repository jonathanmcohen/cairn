/**
 * Plan B3 (#117) — heading collapse chevron (regression; already shipped v0.9.13).
 * Contract stub. Real assertions land with Plan B3.
 * See docs/superpowers/v0.9.14/plan-B-editor-block-fixes.md.
 */
import { describe, it } from 'vitest';

describe('Plan B3 #117 — heading collapse (regression)', () => {
  it.todo('heading-collapse extension is registered in the editor extension list');
  it.todo('collapse chevron affordance renders on a heading');
  it.todo('toggling collapse hides/shows the following sibling blocks');
});
