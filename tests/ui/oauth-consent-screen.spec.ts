/**
 * Plan F (MCP OAuth) — consent screen UI.
 * Contract stub. Real assertions land with Plan F.
 * See docs/superpowers/plans/v0.9.16/plan-F-mcp-oauth.md.
 */
import { describe, it } from 'vitest';

describe('Plan F — OAuth consent screen', () => {
  it.todo('renders the client name + target workspace ("Cursor wants to connect to your Homelab workspace")');
  it.todo('lists requested scopes with the friendly Mint-Token tooltips');
  it.todo('has Allow + Cancel actions; Allow posts the grant');
});
