/**
 * Plan C (#144) — top-sidebar density.
 * Contract stub. Real assertions land with Plan C.
 * See docs/superpowers/plans/v0.9.16/plan-C-top-sidebar-density.md.
 */
import { describe, it } from 'vitest';

describe('Plan C #144 — top sidebar density', () => {
  it.todo('workspace switcher trigger: fine-pointer min-h ~32px, pointer-coarse:min-h-11 preserved');
  it.todo('command-palette button: fine-pointer min-h ~36px + reduced mb, pointer-coarse:min-h-11 preserved');
  it.todo('section headers ("Saved searches"/"Pages") tightened mb + label 11px');
  it.todo('PAGES header row ~28px fine-pointer, coarse floor preserved');
  it.todo('workspace switcher still renders the InlineIcon badge (PR #320) — density change does not drop it');
});
