/**
 * Plan A2 (#1) — /settings/workspace/general loader must not 500.
 * Contract stub. Real assertions land with Plan A2 AFTER runtime repro identifies the cause.
 * See docs/superpowers/v0.9.14/plan-A-critical-hotfixes.md.
 */
import { describe, it } from 'vitest';

describe('Plan A2 #1 — workspace general settings', () => {
  it.todo('reproduce: loader for a seeded workspace currently throws (capture real error)');
  it.todo('loader returns workspace general settings without throwing (post-fix)');
});
