/**
 * Plan A3 — Yjs ↔ API content-write precedence (Option C: document + regression).
 * Contract stub. Real assertions land with Plan A3.
 * See docs/superpowers/v0.9.14/plan-A-critical-hotfixes.md.
 */
import { describe, it } from 'vitest';

describe('Plan A3 — pages.content PATCH precedence', () => {
  it.todo('PATCH /api/v1/pages/:id writes pages.content when no active Yjs doc');
  it.todo('documented precedence: editor/Yjs state wins while a doc is open');
  it.todo('update.ts carries the precedence note + TODO for future Hocuspocus publish');
});
